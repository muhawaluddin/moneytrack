@extends('layouts.app') @section('title',$account->name) @section('heading',$account->name) @section('content')<div class="grid grid-3"><div class="account-card" style="background:linear-gradient(135deg,{{ $account->color }},#173f38)"><span>Saldo saat ini</span><strong class="amount" style="font-size:30px">Rp {{ number_format($account->current_balance,0,',','.') }}</strong><small>{{ $account->bank_name }} · {{ strtoupper($account->type) }}</small></div><div class="card"><div class="eyebrow">Saldo awal</div><div class="metric">Rp {{ number_format($account->opening_balance,0,',','.') }}</div><p class="muted">Diperbarui {{ $account->updated_at->diffForHumans() }}</p></div><div class="card"><div class="eyebrow">Aksi cepat</div><div class="actions" style="margin-top:15px"><a class="btn btn-primary" href="{{ route('transactions.create',['account_id'=>$account->id]) }}">+ Transaksi</a><a class="btn btn-soft" href="{{ route('accounts.edit',$account) }}">Edit</a></div></div></div><div class="card" style="margin-top:18px"><h2>Riwayat mutasi</h2>@forelse($transactions as $transaction)<x-transaction-row :transaction="$transaction"/>@empty<div class="empty">Belum ada mutasi pada akun ini.</div>@endforelse{{ $transactions->links() }}</div>@endsection
