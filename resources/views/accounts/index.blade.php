@extends('layouts.app') @section('title','Sumber Kas') @section('heading','Sumber Kas') @section('content')<div class="actions" style="justify-content:flex-end;margin-bottom:18px"><a class="btn btn-primary" href="{{ route('accounts.create') }}">+ Tambah sumber kas</a></div><div class="grid grid-3">@forelse($accounts as $account)<div><a href="{{ route('accounts.show',$account) }}" class="account-card" style="background:linear-gradient(135deg,{{ $account->color }},#173f38);opacity:{{ $account->is_active?1:.55 }}"><span>{{ $account->name }}</span><strong class="amount" style="font-size:26px">Rp {{ number_format($account->current_balance,0,',','.') }}</strong><small>{{ strtoupper($account->type) }} · {{ $account->is_active?'AKTIF':'DIARSIPKAN' }}</small></a><div class="actions" style="margin-top:8px"><a class="btn btn-soft" href="{{ route('accounts.edit',$account) }}">Edit</a>@if($account->is_active)<form method="post" action="{{ route('accounts.destroy',$account) }}">@csrf @method('delete')<button class="btn btn-danger">Arsipkan</button></form>@endif</div></div>@empty<div class="card empty"><h2>Mulai dari sumber uang Anda</h2><p>Tambahkan rekening, e-wallet, atau kas tunai agar saldo dapat dihitung otomatis.</p><a class="btn btn-primary" href="{{ route('accounts.create') }}">Tambah sumber kas pertama</a></div>@endforelse</div>@endsection
