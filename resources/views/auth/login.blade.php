@extends('layouts.guest') @section('title','Masuk') @section('content')<div class="eyebrow">Selamat datang kembali</div><h1>Masuk ke keuangan Anda</h1><p class="muted">Semua saldo, anggaran, dan transaksi dalam satu tempat.</p><form method="post" action="{{ route('login') }}" class="grid">@csrf<div class="field"><label>Email</label><input class="input" type="email" name="email" value="{{ old('email') }}" required autofocus></div><div class="field"><label>Kata sandi</label><input class="input" type="password" name="password" required></div><label><input type="checkbox" name="remember"> Ingat saya</label><button class="btn btn-primary">Masuk</button></form><p class="muted">Belum punya akun? <a class="positive" href="{{ route('register') }}">Daftar gratis</a></p>@endsection
