@extends('layouts.guest') @section('title','Daftar') @section('content')<div class="eyebrow">Mulai lebih tertata</div><h1>Buat akun MoneyTrack</h1><form method="post" action="{{ route('register') }}" class="grid">@csrf<div class="field"><label>Nama</label><input class="input" name="name" value="{{ old('name') }}" required autofocus></div><div class="field"><label>Email</label><input class="input" type="email" name="email" value="{{ old('email') }}" required></div><div class="field"><label>Kata sandi</label><input class="input" type="password" name="password" required></div><div class="field"><label>Ulangi kata sandi</label><input class="input" type="password" name="password_confirmation" required></div><button class="btn btn-primary">Buat akun</button></form><p class="muted">Sudah terdaftar? <a class="positive" href="{{ route('login') }}">Masuk</a></p>@endsection
